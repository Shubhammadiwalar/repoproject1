/**
 * config.js  –  Frontend configuration
 * Change API_BASE if your backend runs on a different host/port.
 */
const CONFIG = {
  API_BASE: "",          // empty = same origin (backend serves frontend)
  DEFAULT_CONF: 0.25,
};
